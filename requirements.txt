# SQL AI Summarizer — Python dependencies
# Install with: pip install -r requirements.txt

# Web framework
fastapi==0.115.5
uvicorn==0.32.1

# MSSQL connection
# Linux (recommended for SQL Server 2008 R2): pip install pymssql
# Requires: sudo apt-get install freetds-dev freetds-bin
pymssql==2.3.1

# Windows / Mac fallback
pyodbc==5.2.0

# AI providers (all optional — install only what you use)
anthropic==0.40.0     # Claude (Anthropic)
openai==1.57.0        # OpenAI (also works for Groq & OpenRouter via compatible API)

# HTTP requests (for Ollama local AI)
requests==2.32.3

# Serve static files
aiofiles==24.1.0
